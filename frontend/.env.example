# Base URL the frontend hits for API calls. Set this on Vercel to your
# Render service URL once it's deployed (e.g.
# https://agency-arena-api.onrender.com).
NEXT_PUBLIC_API_BASE=http://localhost:8001
